# 部署到静态托管 & 「添加到主屏幕」排障说明

## 一、直接部署

把**整个文件夹**（保持内部结构不变）上传到任意静态托管（GitHub Pages / Netlify /
Vercel / Cloudflare Pages 等），访问 `https://你的域名/index.html` 即可。

无需构建、无需服务器端代码。

## 二、安装入口在哪里（2026-09-06 修复后）

修复后应用**自带安装引导**，不再依赖浏览器菜单：

| 位置 | 形式 |
|---|---|
| 首页顶部 | 蓝色横幅：安卓可一键弹官方安装框；iOS 显示「分享 → 添加到主屏幕」路径；可点 × 永久关闭 |
| 「使用说明」页 | 完整安装卡片：一键安装按钮 + 各平台手动路径图解 |

- **安卓 Chrome / Edge / 三星浏览器**：点按钮 → 官方安装弹窗 → 确认 → 主屏幕出现图标
- **iOS Safari**：无一键安装 API（苹果限制），按卡片指引：底部**分享 ⬆️** → **添加到主屏幕**
- **桌面浏览器**：地址栏右侧出现安装图标，或用菜单

## 三、本次修复了什么（为什么之前菜单里没有安装项）

浏览器判定「可安装」有硬性门槛，之前缺了最关键的两项：

1. **图标只有 SVG** → Chrome 要求 manifest 必须含 **192×192 和 512×512 的 PNG 位图**；
   iOS 的 apple-touch-icon 也完全不认 SVG。已用 `build-icons.py` 从原 SVG 设计栅格化出
   `icon-192 / icon-512 / icon-maskable-512 / apple-touch-icon(180) / favicon-32`。
2. **manifest 换成 `manifest.json`** → 部分免费托管没有 `.webmanifest` 的 MIME 映射
   （按 octet-stream 下发），清单会解析失败。
3. **Service Worker 缓存清单补齐**（新增 `js/mic-permission.js`、`js/install.js`、PNG 图标），
   缓存版本升到 `estalk-v4`。任何一项 404 都会让 SW 永远激活失败 → 判定不可安装。
4. **补 iOS meta**（`apple-mobile-web-app-capable` 等），添加后以独立 App 模式运行。

## 四、部署后仍看不到安装项？逐条排查

1. **必须是 https**（或 localhost）。http 下 SW 无法注册，必然不可安装。
2. **首次访问先刷新一次**：SW 首次注册后需要页面重载取得控制权；
   部分浏览器还要求访问过 1~2 次（“熟悉期”）才在菜单里出现安装项。
   —— 用应用内的安装按钮则不受此限制。
3. **确认托管没有重命名/剥离文件**：检查 `https://域名/manifest.json` 能打开且是 JSON；
   `https://域名/assets/icon-192.png` 能打开是图片。
4. **旧缓存**：修复前访问过的话，浏览器可能记住了旧清单。
   手机浏览器设置里清除该站点数据，或换隐身窗口再试。
5. **iOS 上没有也不会有「安装按钮」**：苹果不提供安装事件 API，属正常，
   按引导卡片的三步操作即可，效果完全一样（全屏 + 离线 + 主屏幕图标）。

## 五、重新生成图标（仅维护时需要）

`python build-icons.py` —— 依赖 Pillow，把 `assets/icon.svg` 的设计重新栅格化成全部 PNG。
